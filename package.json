{
  "name": "securerecon",
  "version": "1.0.0",
  "private": true,
  "description": "SecureRecon - Web Application Security Assessment & Vulnerability Management Platform",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
